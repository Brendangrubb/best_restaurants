<!DOCTYPE html>
<html>
    <head>
        <link href="css/bootstrap.min.css" rel="stylesheet" type="text/css">
        <title>Best Restaurants</title>
    </head>
    <body>
        <div class="container">
            <h1>Welcome to our Restaurant Thing!</h1>

            <p>Create a new cuisine:</p>


            <!-- ADD CUISINE FORM -->
                <form action="/cuisines" method="post">
                    <div class="form-group">
                        <label for="type">Add Type of Cuisine:</label><br>
                        <input class="form-control" id="type" type="text" name="type" required>
                    </div>
                    <button class="btn btn-danger" type="submit">Add Cuisine!</button>
                </form>

                {% if cuisines is not empty %}
                    <p>Here are your cuisines:</p>
                    <ul>
                        {% for cuisine in cuisines %}
                        <li><a href="/cuisine/{{cuisine.getId}}">{{ cuisine.getType }}</a></li>
                        {% endfor %}
                    </ul>
                {% else %}
                    <br>
                    <p>Add some cuisines, dummy!</p>
                {% endif %}


        </div>

    </body>
</html>
